#include <iostream>

using namespace std;

int main()
{
    // freopen("janitorin.txt", "r", stdin);
    // freopen("janitorout.txt", "w", stdout);
    
    int R, C, Q;

    cin >> R;
    cin >> C;
    cin >> Q;

    
}