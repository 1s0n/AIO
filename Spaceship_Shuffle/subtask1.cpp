#include <iostream>

using namespace std;

int main()
{
    // freopen("spacein.txt", "r", stdin);
    // freopen("spaceout.txt", "w", stdout);

    int n;

    cin >> n;

    for (int i = 0; i < n; i++)
    {
        
    }
}